- @page_title = "Returning to the initial state once video playback ends"

%h2= @page_title

:textile

  Sometimes you may want to return to the video's initial state – with the initial start view – once a video has finished playing. You can use the following "player setting":player-settings:

%table#supported_browsers
  %colgroup
    %col
    %col.alt
    %col
    %col.alt
  %tr.alt
    %th Name
    %th Values
    %th Default
    %th Description & Notes
  %tr
    %td
      %strong stop-on-end
    %td <code>false</code><br /><code>true</code>
    %td <code>false</code>
    %td Allows to display the start view when a video playback ends.

:textile
  h3(#demo). "Demo":#demo

  #{jsfiddle_embed('BrHX5', height: 300)}
