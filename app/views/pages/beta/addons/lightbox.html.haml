= content_for :javascript, javascript_include_tag('//cdn.sublimevideo.net/js/12665678.js')

- @page_title = "Add-ons: Lightbox"

%h2= @page_title

:textile
  _The Lightbox add-on is available *completely free of charge for all player editions*._

  The Lightbox add-on displays your videos but also your images in a floating player. The Lightbox-opening trigger is a simple @<a>@ element, and you can have multiple @<a>@ that open the same video or image in a lightbox.

  While it is possible to integrate SublimeVideo with other lightbox libraries (you'll need to use our "JavaScript API":/javascript-api for this), we highly recommend using SublimeVideo's Lightbox add-on to get the easiest and best possible lightbox integration for your videos.

  h3(#step-1). "Step 1: Create a &lt;video&gt; element":#step-1

  bc(language-markup). <video id="video1" style="display:none" width="640" height="360" poster="video-poster.jpg" preload="none">
    <source src="http://yoursite.com/video.mp4" />
    <source src="http://yoursite.com/video.webm" />
  </video>

  * Don't add the _sublime_ @class@ to the @<video>@.
  * The @<video>@ element needs to be initially hidden (with @style="display:none"@ in the code above).

  h3(#step-2). "Step 2: Create an &lt;a&gt; element":#step-2

  bc(language-markup). <a class="sublime" href="#video1">Play the video with the ID "video1"</a>

  * The @href@ value must corresponds to the @id@ of the @<video>@ element created in "Step 1":#step-1.
  * Add the _sublime_ @class@ to the @<a>@.

  h3(#lightbox-settings). "Lightbox settings":#lightbox-settings

  It's easy to customize your lightbox with the following "player settings":player-settings (be sure to set these settings on the @<a>@, not on the @<video>@):

%table#supported_browsers
  %colgroup
    %col
    %col.alt
    %col
    %col.alt
  %tr.alt
    %th Name
    %th Values
    %th Default
    %th Description & Notes
  %tr
    %td
      %strong autoplay
    %td <code>true</code><br /><code>false</code>
    %td <code>true</code>
    %td.notes Automatically plays the video once the lightbox opens.
  %tr.alt
    %td
      %strong overlay-color
    %td <code>literal</code><br /><code>#hexadecimal</code><br /><code>rgb</code>
    %td <code>#000</code>
    %td.notes Customizes the lightbox overlay color.
  %tr
    %td
      %strong overlay-opacity
    %td
      %small <code>0.1 ≤ opacity ≤ 1.0</code>
    %td <code>0.7</code>
    %td.notes Customizes the lightbox overlay opacity.
  %tr.alt
    %td
      %strong close-button
    %td <code>none</code><br /><code>autohide</code><br /><code>sticky</code>
    %td <code>autohide</code>
    %td.notes Allows you to choose between: hiding the lightbox close button, permanently displaying it or displaying it only upon mousover (default).
  %tr
    %td
      %strong close-button-position
    %td <code>left</code><br /><code>right</code>
    %td <code>left</code>
    %td.notes Defines where the lightbox close button should be placed.

:textile
  h3. Demo

  #{jsfiddle_embed('G9748', height: 500)}
​
