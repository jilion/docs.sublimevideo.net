* <span class="label improved">improved</span> Compatibility with existing CSS (box-shadow property).
* <span class="label improved">improved</span> Type attribute parsing of video sources.
* <span class="label improved">improved</span> Back-compatibility support for old not-needed-anymore code (JS API) that was required with the old player to make the video responsive.
* <span class="label fixed">fixed</span> Issue that was flashing the play button just before playing the video.
* <span class="label fixed">fixed</span> Issue that was wrongly configuring the onEnd flag set via the MySublimeVideo.
* <span class="label fixed">fixed</span> Issue that was preventing the lightbox (with the old syntax) from opening.
