* <span class="label new">new</span> Added fullscreen support for stock Android Browser.
* <span class="label improved">improved</span> Compatibility with existing CSS (top, bottom, left and right properties).
* <span class="label fixed">fixed</span> SVG rendering issue on Firefox.
